{
  "name": "pocolente",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "tsup": "^8.0.0"
  }
}
